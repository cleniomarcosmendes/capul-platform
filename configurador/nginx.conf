server {
    listen 5175;
    root /usr/share/nginx/html;
    index index.html;

    location /configurador/ {
        try_files $uri $uri/ /index.html;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
