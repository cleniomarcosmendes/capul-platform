server {
    listen 5174;
    root /usr/share/nginx/html;
    index index.html;

    location /inventario/ {
        try_files $uri $uri/ /index.html;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
