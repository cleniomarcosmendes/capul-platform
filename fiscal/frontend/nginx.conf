server {
    listen 5176;
    root /usr/share/nginx/html;
    index index.html;

    location /fiscal/ {
        alias /usr/share/nginx/html/;
        try_files $uri $uri/ /index.html;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
